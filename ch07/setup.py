from setuptools import setup, find_packages

setup(name="ch07", version="0.1", packages=find_packages())

# for executing this file, we must be placed in our root and the execute the command 'pip install -e .' 
# which -e means 'editable mode'.
